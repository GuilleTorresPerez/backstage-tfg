# CODEOWNERS — generado por la plantilla frontend-angular-desy del IDP
# Las rutas sensibles requieren revisión del equipo de seguridad (RS-OUT-02).

# Configuración sensible (entornos, build, CI)
src/environments/        @security-reviewers
/*.config.ts             @security-reviewers
/angular.json            @security-reviewers
/.gitlab-ci.yml          @security-reviewers
/catalog-info.yaml       @security-reviewers

# Resto del repositorio
*                        @platform-admin
